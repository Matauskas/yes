@model IEnumerable<Org.Ktu.Isk.P175B602.Autonuoma.Models.Salis.SalisL>

@* this will also be used as page title in main layout *@
@{ ViewData["title"] = "Keliones"; }

@* content header *@
<div class="main-content-header">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("Create")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Sukurti naują</a>
	</span>
</div>

@* content itself *@
<div class="main-content-lst">
	<table class="entity-list">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(m => m.Pavadinimas)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Miestas)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Zemynas)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model) {
			<tr>
				<td>
					@Html.DisplayFor(m => item.Pavadinimas)
				</td>
				<td>
					@Html.DisplayFor(m => item.Miestas)
				</td>
				<td>
					@Html.DisplayFor(m => item.Zemynas)
				</td>
				<td>
					<a href="@Url.Action("Edit", new { id = item.Pavadinimas })" class="btn btn-primary"><i class="fa-solid fa-pen-to-square"></i> Redaguoti</a>
					<a href="@Url.Action("Delete", new { id = item.Pavadinimas })" class="btn btn-danger"><i class="fa-solid fa-trash"></i> Šalinti</a>
					<a href="@Url.Action("View", new { id = item.Pavadinimas })" class="btn btn-primary"><i class="fa-solid fa-pen-to-square"></i> Perziureti</a>
				</td>
			</tr>
			}
		</tbody>
	</table>
</div>